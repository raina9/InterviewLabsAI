package com.interviewlab.drill;

public enum DrillMode {
    RAPID,  // 10 quick Q&A, pre-generated questions, AI scores each answer
    DEEP    // Socratic drilling — agent probes deeper based on each answer, up to 8 turns
}
