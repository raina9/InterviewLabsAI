package com.interviewlab.session;

/**
 * Interview format — determines the question strategy in InterviewAgent.
 * Stored as TEXT via @Enumerated(EnumType.STRING).
 */
public enum InterviewType {
    HR,
    TECHNICAL,
    SYSTEM_DESIGN,
    BEHAVIOURAL
}
